# 7gram Dashboard example environment file
# Copy to .env and adjust values.

# Dashboard service
DASHBOARD_PORT=3000
NODE_ENV=development

# (Optional) override for nginx cert paths or domains can be placed in shared-nginx/.env
