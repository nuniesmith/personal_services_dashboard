# Placeholder frontend/dashboard image
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install || true
COPY . .
CMD ["sh","-c","node -e 'http=require("http");http.createServer((_,r)=>r.end("7gram dashboard placeholder" )).listen(3000);' "]
